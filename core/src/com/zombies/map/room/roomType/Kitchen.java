package com.zombies.map.room.roomType;

import com.zombies.map.room.Building;
import com.zombies.map.room.Room;

public class Kitchen extends Room {
    protected Kitchen(Building building, int[] key, int maxBoxes) {
        super(building, key, maxBoxes);
    }
}
